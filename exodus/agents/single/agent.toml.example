[agent]
name = "agent_name"
description = "This is a general purpose agent"
system_prompt = "You are Exodus, an assistant agent focused on cybersecurity"
tools = ["core_example"]
handoffs = ["another_agent"]

# --- Extra Configuration ---
# Any extra keys directly under [agent] will be loaded into the 'config' dictionary.
# These override the global defaults from settings.toml (e.g., max_iterations).
max_iterations = 10

[agent.llm]
temperature = 1
api_key = ""
model = ""
provider = ""
temperature = ""
