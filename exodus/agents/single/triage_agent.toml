[agent]
name = "triage_agent"
description = "Routes user requests to specialized security agents based on task type"
system_prompt = ["common.md", "triage_agent.md"]

tools = []
handoffs = ["recon_agent", "exploit_agent", "privesc_agent", "web_exploit_agent"]

[agent.llm]
temperature = 0.4

