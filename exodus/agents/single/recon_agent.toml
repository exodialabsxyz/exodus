[agent]
name = "recon_agent"
description = "Expert in reconnaissance, enumeration, and information gathering"
system_prompt = ["common.md", "session.md", "recon_agent.md"]

tools = [
    "shell",
    "wait",
]
handoffs = ["triage_agent", "exploit_agent", "web_exploit_agent", "privesc_agent"]

[agent.llm]
temperature = 0.3

